#include<stdio.h>
int fact(int);
int main()
{
    int k;
    k=fact(5);
    printf("factorial of n is %d",k);
    return 0;
}
int fact(int n)
{
    if(n==0 || n==1)
        return 1;
    return n*fact(n-1);

}
